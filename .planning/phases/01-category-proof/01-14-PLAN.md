---
phase: 01-category-proof
plan: "14"
type: execute
wave: 14
depends_on: ["01-13"]
files_modified: [crates/ide-core/src/evidence.rs, crates/ide-core/src/reconciliation.rs, crates/ide-core/tests/evidence_reconciliation.rs, fixtures/aag-provider/src/index.ts, apps/renderer/src/features/evidence/EvidenceView.tsx, apps/renderer/src/features/reconciliation/ReconciliationView.tsx]
autonomous: true
requirements: [LIFE-01]
must_haves:
  truths:
    - "AAG absence/slow/malformed degrades structural evidence to unknown without blocking local work."
    - "The user resolves one bid-privacy divergence by implementation, intent, or scoped exception."
    - "Only passed evidence can verify an outcome."
  artifacts:
    - {path: "crates/ide-core/src/evidence.rs", provides: "Honest evidence ledger and optional AAG port"}
    - {path: "crates/ide-core/src/reconciliation.rs", provides: "Three-authority reconciliation commands"}
  key_links:
    - {from: "fixtures/aag-provider/src/index.ts", to: "crates/ide-core/src/evidence.rs", via: "Validated optional provider evidence or unknown"}
    - {from: "crates/ide-core/src/reconciliation.rs", to: "apps/renderer/src/features/reconciliation/ReconciliationView.tsx", via: "Revision-bound finding and resolution"}
---
<objective>Produce honest evidence and reconcile the concrete sealed-bid privacy divergence.

Purpose: Prove D-04 and R-04 without importing Company Brain scope.
Output: Evidence ledger, degradable AAG provider, privacy finding, three reconciliation routes, and UI.</objective>
<execution_context>@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/HARNESS-SPEC.md
@.planning/KATSUI-BOUNDARY.md
@.planning/phases/01-category-proof/01-13-SUMMARY.md</context>
## Phase Goal
**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Establish evidence honesty and AAG degradation</name>
  <files>crates/ide-core/src/evidence.rs, crates/ide-core/tests/evidence_reconciliation.rs, fixtures/aag-provider/src/index.ts, apps/renderer/src/features/evidence/EvidenceView.tsx</files>
  <behavior>
    - Status is passed, failed, unknown, notRun, or inconclusive; only passed verifies.
    - AAG states affect structural evidence only and never block project/edit/preview/check/reconciliation.
  </behavior>
  <action>Implement local evidence and optional `GraphEvidenceProvider`. Validate AAG schema/provenance/freshness/timeout/size and expose claim, references, confidence, severity, consequence, remediation, scope, verifier/version, and revisions. Make states visually/semantically distinct. AAG cannot author truth per D-04.</action>
  <verify><automated>rtk cargo test -p ide-core --test evidence_reconciliation -- evidence_states &amp;&amp; rtk npm test -- --run EvidenceView fixtures/aag-provider</automated></verify>
  <done>AAG enriches when valid and otherwise degrades honestly while local work remains complete.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Reconcile the bounded privacy divergence</name>
  <files>crates/ide-core/src/reconciliation.rs, crates/ide-core/tests/evidence_reconciliation.rs, apps/renderer/src/features/reconciliation/ReconciliationView.tsx</files>
  <behavior>
    - One editable privacy rule is compared with deterministic real-route behavior evidence.
    - Implementation, intent, and scoped-exception commands modify only their declared authority and retain history.
  </behavior>
  <action>Implement R-04 using the sealed-bid no-leading-amount rule and real benchmark probe. Bind finding to intent/implementation revisions and evidence. Require scope, justification, and expiry/review for exceptions. Do not add generic inference, connectors, or organizational truth.</action>
  <verify><automated>rtk cargo test -p ide-core --test evidence_reconciliation -- reconciliation &amp;&amp; rtk npm test -- --run ReconciliationView</automated></verify>
  <done>The user understands and resolves one real divergence without silent overwrites.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| AAG→evidence→reconciliation | Optional claims can overstate authority or target stale revisions. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-17 | Spoofing | AAG evidence | high | mitigate | Schema/provenance/freshness bounds; absence becomes unknown. |
| T-01-20 | Tampering | reconciliation | high | mitigate | Exact authority/revision targeting and immutable history. |
</threat_model>
<verification>Run all evidence states, AAG degradation, and three reconciliation routes.</verification>
<success_criteria>Evidence is honest and the bounded semantic divergence is explicitly reconcilable.</success_criteria>
<output>Create `.planning/phases/01-category-proof/01-14-SUMMARY.md` when done.</output>

