---
phase: 01-category-proof
plan: "06"
type: execute
wave: 6
depends_on: ["01-05"]
files_modified: [docs/decisions/0001-desktop-shell.md, apps/desktop/README.md, apps/desktop/host.config.json, packages/contracts/src/desktop-extension.ts, tests/contracts/selected-host-conformance.test.ts, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/tauri.conf.json, apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/src/lib.rs, apps/desktop/src-tauri/src/commands.rs, apps/desktop/electron/package.json, apps/desktop/electron/main.ts, apps/desktop/electron/preload.ts, apps/desktop/electron/supervisor.ts, spikes/shell-comparison/results.json]
autonomous: true
requirements: [WORK-04, LIFE-01]
must_haves:
  truths:
    - "Only a candidate passing every hard gate can become the product host."
    - "The selected host is reproducible from evidence and is materialized cleanly as apps/desktop."
  artifacts:
    - {path: "docs/decisions/0001-desktop-shell.md", provides: "Weighted decision, evidence, caveats, and reproduction"}
    - {path: "packages/contracts/src/desktop-extension.ts", provides: "Winner-neutral native extension and lifecycle contract"}
    - {path: "apps/desktop/host.config.json", provides: "Winner plus exact authoritative native command, bridge, preload, and supervisor paths"}
    - {path: "tests/contracts/selected-host-conformance.test.ts", provides: "One conformance suite for either materialized host"}
  key_links:
    - {from: "spikes/shell-comparison/results.json", to: "docs/decisions/0001-desktop-shell.md", via: "Hard-gate eligibility and weighted scoring"}
    - {from: "docs/decisions/0001-desktop-shell.md", to: "apps/desktop/host.config.json", via: "Recorded winner and one exact conditional native file set"}
    - {from: "apps/desktop/host.config.json", to: "tests/contracts/selected-host-conformance.test.ts", via: "Winner-neutral extension contract and recorded concrete paths"}
---

<objective>
Select the desktop host from measured evidence and materialize only the winner.

Purpose: Close R-01 and D-02 before product capabilities depend on a desktop runtime.
Output: ADR, clean selected-host app, and verified package configuration.
</objective>

<execution_context>@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/01-category-proof/01-RESEARCH.md
@.planning/phases/01-category-proof/01-05-SUMMARY.md
@spikes/shell-comparison/results.json
</context>

## Phase Goal

**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.

<tasks>
<task type="auto">
  <name>Task 1: Record the weighted host decision</name>
  <files>docs/decisions/0001-desktop-shell.md, spikes/shell-comparison/results.json</files>
  <action>Exclude candidates failing a hard gate, apply research weights to eligible candidates, and use shared-core fit only for a score gap under 5/100. Record environment, commands, raw evidence, failures, unsupported OS claims, score, winner, and caveats. Do not name a winner if neither candidate is eligible.</action>
  <verify><automated>rtk node spikes/shell-comparison/run-conformance.mjs --verify-adr docs/decisions/0001-desktop-shell.md</automated></verify>
  <done>ADR 0001 names an eligible winner or explicitly blocks downstream execution with evidence.</done>
</task>
<task type="auto">
  <name>Task 2: Materialize and package the selected host</name>
  <files>apps/desktop/README.md, apps/desktop/host.config.json, packages/contracts/src/desktop-extension.ts, tests/contracts/selected-host-conformance.test.ts, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/tauri.conf.json, apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/src/lib.rs, apps/desktop/src-tauri/src/commands.rs, apps/desktop/electron/package.json, apps/desktop/electron/main.ts, apps/desktop/electron/preload.ts, apps/desktop/electron/supervisor.ts</files>
  <action>Define stable `DesktopExtension`, `NativeCommandRegistry`, `ProcessSupervisor`, and typed renderer bridge contracts without importing either framework. Materialize exactly one conditional file set from the ADR: for Tauri, `src-tauri/{Cargo.toml,tauri.conf.json,src/main.rs,src/lib.rs,src/commands.rs}`; for Electron, `electron/{package.json,main.ts,preload.ts,supervisor.ts}`. Record `winner`, `nativeManifest`, `nativeEntrypoint`, `commandRegistry`, `rendererBridge`, and `processSupervisor` as concrete paths in `host.config.json`; use `null` only when the selected architecture genuinely has no separate preload or supervisor and prove the equivalent owner in the command registry. Make the conformance suite load this record, require every declared path to exist, exercise the extension contract, and reject files from the losing conditional set. Verify Linux packaging, then remove both disposable candidate directories per D-02 while retaining shared fixtures/results.</action>
  <verify><automated>rtk node spikes/shell-comparison/run-conformance.mjs --verify-selected-host apps/desktop/host.config.json &amp;&amp; rtk npm test -- --run tests/contracts/selected-host-conformance.test.ts &amp;&amp; rtk test ! -d apps/host-tauri-spike &amp;&amp; rtk test ! -d apps/host-electron-spike</automated></verify>
  <done>One packaged selected host remains and neither candidate tree survives as a product branch.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Evidence→architecture | Incomplete or biased measurements could select an unsafe host. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-25 | Tampering | host ADR | high | mitigate | Machine-validated raw-result links, hard-gate exclusion, reproducible commands. |
</threat_model>
<verification>Validate the ADR, selected-host manifest paths, package command, and removal of both candidate trees.</verification>
<success_criteria>The runtime decision is closed by reproducible evidence and downstream work has one executable host.</success_criteria>
<output>Create `.planning/phases/01-category-proof/01-06-SUMMARY.md` when done.</output>
