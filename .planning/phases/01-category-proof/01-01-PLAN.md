---
phase: 01-category-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PROJ-01, WORK-04, INTN-01, CONF-01, LIFE-01]
must_haves:
  truths:
    - "The exact fresh package releases are human-verified against their official registries before any install."
  artifacts: []
  key_links:
    - "Registry identity and source repository must agree for every freshness-flagged package."
---

<objective>
Authorize the dependency set required by the vertical walking skeleton after checking every freshness-flagged package.

Purpose: Satisfy the package-legitimacy and supply-chain gate before Plans 01-02 and 01-03 install executable tooling, while preserving D-01, D-02, and D-03.
Output: A recorded human approval or a corrected version selection supplied to the executor.
</objective>

<execution_context>
@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-category-proof/01-CONTEXT.md
@.planning/phases/01-category-proof/01-RESEARCH.md
</context>

## Phase Goal

**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify freshness-flagged package identities</name>
  <files>None — blocking human supply-chain gate</files>
  <action>Pause before dependency installation and require the registry/source checks below. Resume only with explicit approval or exact replacement versions.</action>
  <what-built>The research package-legitimacy audit identified fresh releases that require human confirmation before installation: Vite 8.2.2, Vitest 4.1.11, Playwright 1.62.1, and Electron 43.4.1.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vite/v/8.2.2 and confirm the package links to `github.com/vitejs/vite`.
    2. Open https://www.npmjs.com/package/vitest/v/4.1.11 and confirm the package links to `github.com/vitest-dev/vitest`.
    3. Open https://www.npmjs.com/package/@playwright/test/v/1.62.1 and confirm the package links to `github.com/microsoft/playwright`.
    4. Open https://www.npmjs.com/package/electron/v/43.4.1 and confirm the package links to `github.com/electron/electron`.
    5. Confirm use of those versions, or provide corrected exact versions from the same official packages. No dependency installation may begin before this approval.
  </how-to-verify>
  <verify><automated>rtk test ! -f package-lock.json</automated></verify>
  <done>Human approval or replacement versions are recorded before any dependency lockfile is created.</done>
  <resume-signal>Type "approved" or provide replacement package versions.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry→local toolchain | Third-party executable build and test packages enter the workspace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | npm dependencies | high | mitigate | Blocking registry/source identity confirmation before installation; preserve the approved versions in lockfiles. |
</threat_model>

<verification>
The executor must not continue to an install plan until the blocking-human checkpoint is approved.
</verification>

<success_criteria>
The exact versions of all `[SUS]` packages have explicit human approval or explicit replacements.
</success_criteria>

<output>
Create `.planning/phases/01-category-proof/01-01-SUMMARY.md` when done.
</output>
