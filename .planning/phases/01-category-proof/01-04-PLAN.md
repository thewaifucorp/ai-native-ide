---
phase: 01-category-proof
plan: "04"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified: [crates/shell-spike-fixture/Cargo.toml, crates/shell-spike-fixture/src/lib.rs, crates/shell-spike-fixture/src/bin/fixture-daemon.rs, tests/contracts/host-conformance.test.ts, spikes/shell-comparison/rubric.json, spikes/shell-comparison/run-conformance.mjs]
autonomous: true
requirements: [WORK-04, LIFE-01]
must_haves:
  truths:
    - "Both desktop candidates are judged by one shared fixture, operation sequence, and rubric."
    - "The shared Rust fixture is callable in-process and through one framed daemon without behavior drift."
  artifacts:
    - {path: "crates/shell-spike-fixture/src/lib.rs", provides: "Shared privileged comparison operations"}
    - {path: "tests/contracts/host-conformance.test.ts", provides: "Candidate-neutral executable contract"}
  key_links:
    - {from: "crates/shell-spike-fixture/src/bin/fixture-daemon.rs", to: "crates/shell-spike-fixture/src/lib.rs", via: "Framed serialization around identical library calls"}
    - {from: "spikes/shell-comparison/run-conformance.mjs", to: "tests/contracts/host-conformance.test.ts", via: "One fixture and rubric"}
---

<objective>
Define the invariant fixture and conformance rubric before either host candidate is implemented.

Purpose: Prevent the Tauri/Electron experiment from favoring a host, per D-02 and D-03.
Output: Shared Rust fixture, daemon target, conformance test, and weighted rubric runner.
</objective>

<execution_context>@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/01-category-proof/01-CONTEXT.md
@.planning/phases/01-category-proof/01-RESEARCH.md
@.planning/phases/01-category-proof/01-03-SUMMARY.md
@packages/contracts/src/index.ts
</context>

## Phase Goal

**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the shared Rust comparison fixture</name>
  <files>crates/shell-spike-fixture/Cargo.toml, crates/shell-spike-fixture/src/lib.rs, crates/shell-spike-fixture/src/bin/fixture-daemon.rs</files>
  <behavior>
    - Library and `fixture-daemon` expose identical scoped file/watch, PTY, preview, cleanup, and DTO behavior.
    - Framing rejects malformed, oversized, and unknown requests.
  </behavior>
  <action>Implement one Rust crate for all privileged slice operations and an explicit `fixture-daemon` binary target in Cargo metadata. The daemon is a length-delimited stdin/stdout wrapper only; Electron may not substitute Node behavior and Tauri may not fork library behavior. Bound output and make cleanup observations explicit.</action>
  <verify><automated>rtk cargo test -p shell-spike-fixture &amp;&amp; rtk cargo build -p shell-spike-fixture --bin fixture-daemon --release</automated></verify>
  <done>Both invocation modes pass the same fixture tests and the release daemon binary exists.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Freeze candidate-neutral conformance and scoring</name>
  <files>tests/contracts/host-conformance.test.ts, spikes/shell-comparison/rubric.json, spikes/shell-comparison/run-conformance.mjs</files>
  <behavior>
    - The exact operation list covers renderer, Monaco disposal, scoped file watch, 10 MB PTY, preview isolation, cleanup, packaging, and reproducibility.
    - Results require 10 post-warmup release launches and record median/p95 cold start, RSS, artifact size, IPC, build time, and failures.
  </behavior>
  <action>Encode the research weights and hard gates once. Parameterize the runner by candidate command while forbidding candidate-specific assertions. Result validation must reject missing measurements, mismatched renderer/fixture hashes, different machine/commit/build mode, or a claimed pass after a hard-gate failure.</action>
  <verify><automated>rtk npm test -- --run tests/contracts/host-conformance.test.ts &amp;&amp; rtk node spikes/shell-comparison/run-conformance.mjs --verify-rubric</automated></verify>
  <done>The comparison protocol is fixed before either candidate can produce results.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Host→fixture daemon | Candidate-controlled framed input reaches Rust authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-04 | Elevation of privilege | fixture protocol | critical | mitigate | Closed DTOs, length bounds, resource scope, no generic commands. |
| T-01-06 | Denial of service | process fixture | high | mitigate | Bounded streams, timeouts, owned cleanup, observed exits. |
</threat_model>
<verification>Build both fixture targets and run contract/rubric tests without a candidate.</verification>
<success_criteria>The host comparison has an executable invariant baseline that candidates cannot redefine.</success_criteria>
<output>Create `.planning/phases/01-category-proof/01-04-SUMMARY.md` when done.</output>

