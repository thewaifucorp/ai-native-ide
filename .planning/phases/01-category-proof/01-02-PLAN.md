---
phase: 01-category-proof
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [package.json, package-lock.json, tsconfig.json, vite.config.ts, eslint.config.js, playwright.config.ts, packages/contracts/src/index.ts, tests/journey/category-proof.spec.ts, scripts/verify-failing-journey.mjs]
autonomous: true
requirements: [PROJ-01, WORK-04, INTN-01, CONF-01, LIFE-01]
must_haves:
  truths:
    - "The complete category journey has a failing-first executable contract before product behavior exists."
    - "All renderer/host traffic uses one runtime-validated shell-neutral contract."
  artifacts:
    - {path: "tests/journey/category-proof.spec.ts", provides: "Failing golden-journey contract with the exact MISSING_INTENT_ENTRY marker"}
    - {path: "scripts/verify-failing-journey.mjs", provides: "Portable failing-first verifier that distinguishes the expected assertion from infrastructure failures"}
    - {path: "packages/contracts/src/index.ts", provides: "Zod-validated host DTOs"}
  key_links:
    - {from: "tests/journey/category-proof.spec.ts", to: "packages/contracts/src/index.ts", via: "Stable domain actions and events"}
---

<objective>
Create the approved workspace, shell-neutral boundary, and failing end-to-end contract.

Purpose: Establish the vertical walking-skeleton spine required by D-01 and the Rust-oriented privilege seam required by D-03 before UI or host implementation.
Output: Locked dependencies, build/test configuration, typed contracts, and an intentionally red Phase 1 journey.
</objective>

<execution_context>
@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-category-proof/01-CONTEXT.md
@.planning/phases/01-category-proof/01-RESEARCH.md
@.planning/phases/01-category-proof/01-01-SUMMARY.md
</context>

## Phase Goal

**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the toolchain and write the failing journey</name>
  <files>package.json, package-lock.json, tsconfig.json, vite.config.ts, eslint.config.js, playwright.config.ts, tests/journey/category-proof.spec.ts, scripts/verify-failing-journey.mjs</files>
  <behavior>
    - Natural-language intent begins the journey without stack, folder, or cloud questions.
    - The journey requires guidance, Essential and Raw, permission, real mutation, preview failure/recovery, evidence, reconciliation, and optional Game Mode.
    - The first run fails specifically because intent entry is not implemented.
  </behavior>
  <action>Install only the versions approved in 01-01 and define lint, typecheck, unit, journey, and build scripts. Write the browser-level golden journey using accessible roles and stable domain IDs. Make its first intentionally failing assertion emit the exact marker `MISSING_INTENT_ENTRY` and no other assertion may use that marker. Create `scripts/verify-failing-journey.mjs` using Node `spawnSync`: run `npm run typecheck` and require exit 0; independently run the category-proof journey, require its nonzero exit, require exactly one `MISSING_INTENT_ENTRY`, require the named missing-intent Playwright assertion, and reject configuration, syntax, missing-browser, server-start, timeout, or unrelated assertion failures. Keep the scenario host-neutral per D-01 and D-02.</action>
  <verify><automated>rtk npm run typecheck &amp;&amp; rtk node scripts/verify-failing-journey.mjs</automated></verify>
  <done>The toolchain is locked and the complete journey is red for the first missing product behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the validated IdeHost contract</name>
  <files>packages/contracts/src/index.ts</files>
  <behavior>
    - Requests/events cover project, resource, activity, effect, preview, evidence, reconciliation, and verified outcome.
    - Malformed and unknown messages are rejected at runtime.
    - Project identity is independent of repository and session.
  </behavior>
  <action>Define closed discriminated Zod schemas and inferred TypeScript types. Include stable project/resource/session/effect/run/evidence IDs, artifact revisions, causation, and honest evidence states. Do not expose generic IPC, paths as authority, or desktop SDK objects. Preserve semantic project identity per D-05 and provider neutrality per D-11.</action>
  <verify><automated>rtk npm test -- --run packages/contracts &amp;&amp; rtk npm run typecheck</automated></verify>
  <done>The contract is shell-neutral, runtime validated, and expressive enough for the complete journey.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Renderer→host | Untrusted UI messages request privileged effects. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Elevation of privilege | IdeHost contract | high | mitigate | Closed named DTOs, runtime validation, no generic IPC. |
| T-01-SC | Tampering | package installs | high | mitigate | Install only human-approved exact releases and lock them. |
</threat_model>

<verification>Run contract tests, typecheck, and confirm the journey is red only at the intended first assertion.</verification>
<success_criteria>The phase has a reproducible failing-first spine and a narrow validated host boundary.</success_criteria>
<output>Create `.planning/phases/01-category-proof/01-02-SUMMARY.md` when done.</output>
