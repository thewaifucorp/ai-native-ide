---
phase: 01-category-proof
plan: "11"
type: execute
wave: 11
depends_on: ["01-10"]
files_modified: [crates/ide-core/src/policy.rs, crates/ide-core/src/checkpoint.rs, crates/ide-core/src/effect_dispatcher.rs, packages/adapter-contract/src/index.ts, packages/contracts/src/desktop-extension.ts, apps/desktop/host.config.json, apps/desktop/src-tauri/src/commands.rs, apps/desktop/electron/main.ts, apps/desktop/electron/preload.ts, apps/desktop/electron/supervisor.ts, apps/renderer/src/features/context/ContextDock.tsx, apps/renderer/src/features/activity/ActivityStrip.tsx, tests/contracts/selected-host-conformance.test.ts, tests/journey/effect-broker.spec.ts]
autonomous: true
requirements: [LIFE-01]
must_haves:
  truths:
    - "One exact mutation pauses in Context Dock and supports deny, allow-once, invalidation, and rollback."
    - "Authorization never becomes evidence or Game Mode progress."
  artifacts:
    - {path: "crates/ide-core/src/policy.rs", provides: "Effect-time balanced broker"}
    - {path: "crates/ide-core/src/checkpoint.rs", provides: "Reversible pre-effect snapshot"}
    - {path: "crates/ide-core/src/effect_dispatcher.rs", provides: "Only mutation dispatcher from agent proposal through policy and ide-core resource authority"}
    - {path: "tests/journey/effect-broker.spec.ts", provides: "Selected-host E2E proving proposal pause, exact approval binding, mutation, observation, and rollback"}
  key_links:
    - {from: "apps/renderer/src/features/context/ContextDock.tsx", to: "crates/ide-core/src/policy.rs", via: "Decision bound to effect/session/resource/revision"}
    - {from: "crates/ide-core/src/checkpoint.rs", to: "apps/renderer/src/features/activity/ActivityStrip.tsx", via: "Causal mutation and rollback events"}
    - {from: "packages/adapter-contract/src/index.ts", to: "crates/ide-core/src/effect_dispatcher.rs", via: "ProposedEffect enters the sole effect dispatcher before any resource adapter"}
    - {from: "crates/ide-core/src/effect_dispatcher.rs", to: "apps/desktop/host.config.json", via: "Selected native command path invokes ide-core resource mutation only after bound approval"}
---
<objective>Broker the selected agent's first real mutation through one reversible canonical permission.

Purpose: Prove D-06, D-09, and D-10 in the controlled construction slice.
Output: Policy, checkpoint, Context Dock decision, and causal Activity Strip.</objective>
<execution_context>@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/HARNESS-SPEC.md
@.planning/phases/01-category-proof/01-10-SUMMARY.md</context>
## Phase Goal
**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Broker and roll back one effect</name>
  <files>crates/ide-core/src/policy.rs, crates/ide-core/src/checkpoint.rs, crates/ide-core/src/effect_dispatcher.rs, packages/adapter-contract/src/index.ts, packages/contracts/src/desktop-extension.ts, apps/desktop/host.config.json, apps/desktop/src-tauri/src/commands.rs, apps/desktop/electron/main.ts, apps/desktop/electron/preload.ts, apps/desktop/electron/supervisor.ts, apps/renderer/src/features/context/ContextDock.tsx, apps/renderer/src/features/activity/ActivityStrip.tsx, tests/contracts/selected-host-conformance.test.ts, tests/journey/effect-broker.spec.ts</files>
  <behavior>
    - Proposal binds consequence, path, session, revision, effect ID, expiry, and checkpoint.
    - Deny causes no write; changed effect invalidates allow-once; rollback restores state and emits activity.
    - Permission produces no evidence pass or reward.
  </behavior>
  <action>Make the real agent adapter emit `ProposedEffect` into `effect_dispatcher.rs`; remove every direct adapter-to-file mutation route. The dispatcher must evaluate deterministic balanced policy, publish the pending proposal to the canonical Context Dock, and stop until a decision bound to effect ID, session, resource, path, operation digest, base revision, and expiry arrives. On allow-once, create the pre-effect snapshot and invoke the selected host's recorded Tauri command or Electron main/preload/supervisor route, which in turn may mutate only through ide-core resource authorization; on deny or stale/changed approval, perform no mutation. Emit proposal→decision→checkpoint→execution→filesystem observation and rollback events to Activity Strip. Extend selected-host conformance and the E2E to attempt direct adapter/bridge mutation and assert rejection, then prove exact approval, observed mutation, rollback, and invalidated replay. Exclude authorization from verification/progress per D-10.</action>
  <verify><automated>rtk cargo test -p ide-core --lib policy &amp;&amp; rtk cargo test -p ide-core --lib checkpoint &amp;&amp; rtk cargo test -p ide-core --lib effect_dispatcher &amp;&amp; rtk npm test -- --run tests/contracts/selected-host-conformance.test.ts &amp;&amp; rtk npm run test:journey -- --grep "effect broker cannot be bypassed"</automated></verify>
  <done>The real agent edit is precisely authorized, reversible, auditable, and never rewarded merely for approval.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| User decision→effect | Approval could be replayed for a changed operation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-12 | Spoofing | permission replay | high | mitigate | Bind effect, session, scope, digest, revision, expiry; invalidate any change. |
</threat_model>
<verification>Exercise deny, allow, changed effect, rollback, and causal timeline through the selected agent.</verification>
<success_criteria>Controlled construction is real, canonical, reversible, and causally visible.</success_criteria>
<output>Create `.planning/phases/01-category-proof/01-11-SUMMARY.md` when done.</output>
