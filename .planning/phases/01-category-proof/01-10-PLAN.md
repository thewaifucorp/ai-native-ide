---
phase: 01-category-proof
plan: "10"
type: execute
wave: 10
depends_on: ["01-09"]
files_modified: [packages/adapter-contract/src/index.ts, fixtures/fake-agent/src/main.ts, packages/contracts/src/desktop-extension.ts, apps/desktop/host.config.json, apps/desktop/src-tauri/src/commands.rs, apps/desktop/electron/main.ts, apps/desktop/electron/preload.ts, apps/desktop/electron/supervisor.ts, tests/contracts/selected-host-conformance.test.ts, tests/contracts/agent-conformance.test.ts]
autonomous: true
requirements: [LIFE-01]
must_haves:
  truths:
    - "ACP and CLI/PTTY publish evidence-backed auth/create/stream/cancel/resume/effect capabilities."
    - "One real eligible path drives the journey; absent guarantees remain unsupported or degraded."
  artifacts:
    - {path: "packages/adapter-contract/src/index.ts", provides: "Honest normalized agent boundary"}
    - {path: "tests/contracts/agent-conformance.test.ts", provides: "ACP/CLI capability evidence"}
  key_links:
    - {from: "packages/adapter-contract/src/index.ts", to: "apps/desktop/host.config.json", via: "Recorded selected-host command and supervisor owners"}
---
<objective>Evaluate ACP and CLI/PTTY and select one honest real-agent path.

Purpose: Close R-02 without pretending unsupported capabilities.
Output: Adapter contract, fake agent, real probes, selected path, and conformance evidence.</objective>
<execution_context>@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-category-proof/01-RESEARCH.md
@.planning/phases/01-category-proof/01-09-SUMMARY.md</context>
## Phase Goal
**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Probe and select the real agent path</name>
  <files>packages/adapter-contract/src/index.ts, fixtures/fake-agent/src/main.ts, packages/contracts/src/desktop-extension.ts, apps/desktop/host.config.json, apps/desktop/src-tauri/src/commands.rs, apps/desktop/electron/main.ts, apps/desktop/electron/preload.ts, apps/desktop/electron/supervisor.ts, tests/contracts/selected-host-conformance.test.ts, tests/contracts/agent-conformance.test.ts</files>
  <behavior>
    - Capabilities report supported, unsupported, or degraded with observed evidence.
    - Cancel completes only after exit; resume requires the same durable native session identity.
    - Eligibility requires create, stream, observed cancel, and one scoped edit.
  </behavior>
  <action>Define the adapter snapshot and normalized events while keeping ACP at the edge. Probe one locally available ACP agent and one CLI/PTTY agent with adapter-owned auth/config and an explicit environment allowlist. Route process creation and streaming through the winner recorded in `host.config.json`, modifying only Tauri `src-tauri/src/commands.rs` or Electron `electron/{main.ts,preload.ts,supervisor.ts}`, and extend the winner-neutral extension conformance suite. Select an eligible path, retain the other as recorded compatibility evidence, and make fake-agent pass the same deterministic contract. Never create the losing host tree or simulate missing resume/pre-effect control.</action>
  <verify><automated>rtk npm test -- --run tests/contracts/agent-conformance.test.ts tests/contracts/selected-host-conformance.test.ts</automated></verify>
  <done>One real path is selected by observed capability evidence and the UI can declare every degradation before use.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Agent process→IDE | Trusted protocol peer is untrusted for authority and secrets. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-10 | Elevation of privilege | agent adapter | critical | mitigate | No self-authorization; capability declaration and later broker enforcement. |
| T-01-11 | Information disclosure | process environment | high | mitigate | Explicit environment allowlist and adapter-owned auth. |
</threat_model>
<verification>Run deterministic and real-agent conformance, preserving the capability matrix in the summary.</verification>
<success_criteria>The journey has one real interchangeable path whose guarantees are observed rather than inferred.</success_criteria>
<output>Create `.planning/phases/01-category-proof/01-10-SUMMARY.md` when done.</output>
