---
phase: 01-category-proof
plan: "12"
type: execute
wave: 12
depends_on: ["01-11"]
files_modified: [Cargo.toml, crates/benchmark-domain/Cargo.toml, crates/benchmark-domain/src/lib.rs, crates/benchmark-domain/src/bin/benchmark-domain.rs, crates/benchmark-domain/tests/auction_race.rs, apps/benchmark/src/server.ts, apps/benchmark/src/App.tsx]
autonomous: true
requirements: [LIFE-01]
must_haves:
  truths:
    - "Two independent concurrent bids commit one deterministic winner without revealing the leading amount."
    - "The TypeScript server delegates to one explicitly named Rust benchmark-domain executable."
  artifacts:
    - {path: "crates/benchmark-domain/src/bin/benchmark-domain.rs", provides: "Framed transactional benchmark command binary"}
    - {path: "crates/benchmark-domain/tests/auction_race.rs", provides: "Repeated race and secrecy proof"}
  key_links:
    - {from: "apps/benchmark/src/server.ts", to: "crates/benchmark-domain/src/bin/benchmark-domain.rs", via: "Supervised `target/release/benchmark-domain` framed transaction route"}
---
<objective>Make the sealed-bid benchmark executable through one concrete transactional Rust command route.

Purpose: Supply the real microsaaS and concurrency proof required by LIFE-01.
Output: Benchmark domain library/binary, race tests, TypeScript HTTP server, and browser UI.</objective>
<execution_context>@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-category-proof/01-RESEARCH.md
@.planning/phases/01-category-proof/01-11-SUMMARY.md</context>
## Phase Goal
**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prove the browser-to-Rust sealed-bid transaction</name>
  <files>Cargo.toml, crates/benchmark-domain/Cargo.toml, crates/benchmark-domain/src/lib.rs, crates/benchmark-domain/src/bin/benchmark-domain.rs, crates/benchmark-domain/tests/auction_race.rs, apps/benchmark/src/server.ts, apps/benchmark/src/App.tsx</files>
  <behavior>
    - At least 100 barrier-synchronized races use separate SQLite connections and retain two receipts with one deterministic winner.
    - Public responses/logs/activity/screenshots/evidence never reveal the leading amount.
    - `/api/bids` validates input, supervises `target/release/benchmark-domain`, correlates request IDs, and returns the redacted DTO.
  </behavior>
  <action>Implement `BEGIN IMMEDIATE`, bounded busy retry, deterministic tie-break, constraints, and a public DTO allowlist. Declare the exact `benchmark-domain` binary target in Cargo and implement its length-delimited stdin/stdout command protocol. Make the TypeScript server supervise that binary; it must not contain winner logic. Exercise two browser requests through `/api/bids` and assert redaction at every boundary.</action>
  <verify><automated>rtk cargo build -p benchmark-domain --bin benchmark-domain --release &amp;&amp; rtk cargo test -p benchmark-domain --test auction_race -- --test-threads=1 &amp;&amp; rtk npm test -- --run apps/benchmark</automated></verify>
  <done>The exact UI→server→Rust binary→SQLite path is concurrency-correct and secrecy-preserving.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Bidder→server→Rust binary | Concurrent untrusted bids cross process/storage boundaries. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-14 | Tampering | bid transaction | critical | mitigate | Immediate transaction, independent connections, constraints, deterministic tie-break, bounded retry. |
| T-01-15 | Information disclosure | bid DTO/logs | critical | mitigate | Public allowlist and redaction assertions at every boundary. |
</threat_model>
<verification>Build the named binary and run race plus browser-route tests.</verification>
<success_criteria>The benchmark has one supervised transactional implementation and no bid oracle.</success_criteria>
<output>Create `.planning/phases/01-category-proof/01-12-SUMMARY.md` when done.</output>
