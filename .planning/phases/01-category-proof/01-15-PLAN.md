---
phase: 01-category-proof
plan: "15"
type: execute
wave: 15
depends_on: ["01-14"]
files_modified: [packages/projections/src/game-mode.ts, packages/projections/src/game-mode.test.ts, apps/renderer/src/features/activity/ActivityStrip.tsx, tests/journey/category-proof.spec.ts, docs/phase-01-local-run.md]
autonomous: true
requirements: [PROJ-01, WORK-04, INTN-01, CONF-01, LIFE-01]
must_haves:
  truths:
    - "One verifier-owned passed outcome creates one evidence-linked receipt; authorization/activity never progress."
    - "The complete local journey passes with Game Mode on and off and no required external service."
  artifacts:
    - {path: "packages/projections/src/game-mode.ts", provides: "Evidence-gated replay-safe receipt projector"}
    - {path: "tests/journey/category-proof.spec.ts", provides: "Passing complete Phase 1 proof"}
  key_links:
    - {from: "crates/ide-core/src/evidence.rs", to: "packages/projections/src/game-mode.ts", via: "Verifier-owned passed OutcomeVerified events"}
    - {from: "tests/journey/category-proof.spec.ts", to: "docs/phase-01-local-run.md", via: "Same selected-host local route"}
---
<objective>Close the complete category journey and gate optional Game Mode on verified outcomes only.

Purpose: Demonstrate all Phase 1 requirements and D-01 through D-11 in one reproducible run.
Output: Game projector, passing on/off journeys, full gates, and local run guide.</objective>
<execution_context>@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/UI-SPEC.md
@.planning/phases/01-category-proof/01-14-SUMMARY.md</context>
## Phase Goal
**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Project only verified outcome receipts</name>
  <files>packages/projections/src/game-mode.ts, packages/projections/src/game-mode.test.ts, apps/renderer/src/features/activity/ActivityStrip.tsx</files>
  <behavior>
    - Permission, prompts, tokens, writes, terminal, time, failed, unknown, notRun, and inconclusive never progress.
    - A passed event with criterion/evidence/verifier/revisions creates one idempotent receipt; regression invalidates current projection but preserves history.
    - Disabling Game Mode hides cosmetics only.
  </behavior>
  <action>Implement a pure reducer subscribed only to validated verifier-owned outcome events. Render receipts/evidence/regression in Activity Strip. Exclude authorization per D-10 and all activity/cost metrics from reward inputs.</action>
  <verify><automated>rtk npm test -- --run packages/projections/src/game-mode.test.ts ActivityStrip</automated></verify>
  <done>Game Mode rewards exactly one verified result and cannot alter capability.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Pass and document the complete local journey</name>
  <files>tests/journey/category-proof.spec.ts, docs/phase-01-local-run.md</files>
  <behavior>
    - Intent, incremental guidance, real agent mutation, canonical permission, preview failure/recovery, evidence, reconciliation, and receipt execute end to end.
    - Game Mode off exposes identical functional controls.
    - Clean/idle runs need no Katsui, ShinAI, cloud, subscription, marketplace, or paid idle inference.
  </behavior>
  <action>Complete the original failing assertions against the selected real host/core/agent/benchmark route. Trigger guidance after an observed effect and preview failure for INTN-01. Replay with Game Mode off. Document exact `rtk` setup, lint, typecheck, tests, desktop/preview, Linux package, local data, and safe explicit cleanup commands. Read host/agent selections from recorded evidence rather than assuming them.</action>
  <verify><automated>rtk npm run lint &amp;&amp; rtk npm run typecheck &amp;&amp; rtk npm test -- --run &amp;&amp; rtk cargo test --workspace &amp;&amp; rtk npm run test:journey -- --grep "category proof" &amp;&amp; rtk npm run build</automated><human-check>Follow the guide from a clean local data directory with Game Mode on and off; capabilities match and the receipt follows passed evidence only.</human-check></verify>
  <done>All five Phase 1 requirements pass in one free local desktop journey.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Evidence→Game Mode | Forged/uncertain/replayed checks attempt progress. |
| Documentation→local machine | Run/cleanup commands affect processes and data. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-18 | Spoofing | Game outcome | high | mitigate | Passed verifier events with evidence/revision references and idempotence. |
| T-01-23 | Denial of service | cleanup docs | medium | mitigate | Explicit project paths/processes; no broad or unresolved deletion target. |
</threat_model>
<verification>Run all TypeScript/Rust/journey/build gates and the documented on/off clean run.</verification>
<success_criteria>The category proof is complete, reproducible, model-neutral, local, and outcome-gated.</success_criteria>
<output>Create `.planning/phases/01-category-proof/01-15-SUMMARY.md` when done.</output>
