---
phase: 01-category-proof
plan: "08"
type: execute
wave: 8
depends_on: ["01-07"]
files_modified: [crates/ide-daemon/Cargo.toml, crates/ide-daemon/src/main.rs, packages/contracts/src/desktop-extension.ts, apps/desktop/host.config.json, apps/desktop/src-tauri/src/commands.rs, apps/desktop/electron/main.ts, apps/desktop/electron/preload.ts, apps/desktop/electron/supervisor.ts, tests/contracts/selected-host-conformance.test.ts, apps/renderer/src/features/resources/ResourceView.tsx, apps/renderer/src/features/workspace/WorkSurface.tsx]
autonomous: true
requirements: [PROJ-01, CONF-01, LIFE-01]
must_haves:
  truths:
    - "The selected desktop host creates the real semantic project and edits observed files through ide-core."
    - "Raw editor models are keyed by resource identity and external changes appear without reimport."
  artifacts:
    - {path: "crates/ide-daemon/src/main.rs", provides: "Explicit out-of-process ide-core command target"}
    - {path: "apps/desktop/host.config.json", provides: "Recorded selected-host owners extended with project/resource commands"}
  key_links:
    - {from: "apps/desktop/host.config.json", to: "crates/ide-daemon/src/main.rs", via: "Recorded Tauri command registry or Electron main/preload/supervisor route"}
    - {from: "apps/renderer/src/features/resources/ResourceView.tsx", to: "packages/contracts/src/desktop-extension.ts", via: "Validated selected-host project/resource commands"}
---
<objective>Connect the selected desktop host and Instrument UI to real project/resources.

Purpose: Turn the intent slice into a user-visible real-files vertical slice.
Output: Exact daemon target, selected-host adapter, Resource view, and Monaco-backed Work Surface.</objective>
<execution_context>@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-category-proof/01-07-SUMMARY.md
@docs/decisions/0001-desktop-shell.md
@packages/contracts/src/index.ts</context>
## Phase Goal
**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Expose ide-core through the selected host</name>
  <files>crates/ide-daemon/Cargo.toml, crates/ide-daemon/src/main.rs, packages/contracts/src/desktop-extension.ts, apps/desktop/host.config.json, apps/desktop/src-tauri/src/commands.rs, apps/desktop/electron/main.ts, apps/desktop/electron/preload.ts, apps/desktop/electron/supervisor.ts, tests/contracts/selected-host-conformance.test.ts</files>
  <behavior>
    - Electron supervises the explicit `ide-daemon` binary; Tauri calls equivalent typed Rust commands in process.
    - Requests and responses are validated on both sides and renderer privilege remains absent.
  </behavior>
  <action>Define the exact `ide-daemon` Cargo binary and framed protocol. Read `host.config.json`, then modify only its recorded winner paths: add direct typed project/resource commands to Tauri `src-tauri/src/commands.rs`, or add Electron handlers plus validated preload exposure in `electron/main.ts`, `electron/preload.ts`, and daemon ownership in `electron/supervisor.ts`. Extend the winner-neutral contract and conformance suite for the same operations. Never create the losing file set. Validate all DTOs and retain fake-host injection for tests.</action>
  <verify><automated>rtk cargo build -p ide-daemon --bin ide-daemon --release &amp;&amp; rtk cargo test --workspace &amp;&amp; rtk npm run typecheck &amp;&amp; rtk npm test -- --run tests/contracts/selected-host-conformance.test.ts</automated></verify>
  <done>The selected host invokes the real core through a concrete executable route.</done>
</task>
<task type="auto">
  <name>Task 2: Operate real resources in Raw depth</name>
  <files>apps/renderer/src/features/resources/ResourceView.tsx, apps/renderer/src/features/workspace/WorkSurface.tsx</files>
  <action>Replace fake project/resource calls with the real host adapter. Show the benchmark repo as a resource inside the project, never as project identity. Key Monaco models by resource ID plus relative path, dispose models/listeners, surface external observations in Activity Strip/Raw evidence, and preserve Essential state.</action>
  <verify><automated>rtk npm run test:journey -- --grep "real semantic project"</automated></verify>
  <done>The user creates the real project, edits a real scoped file, and sees an external edit without losing identity.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Desktop bridge→ide-core | Renderer identifiers request file effects. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-09 | Information disclosure | file responses | high | mitigate | Core-side resource authorization and narrow DTOs. |
</threat_model>
<verification>Build the exact daemon target, run workspace tests, and execute the real-project journey.</verification>
<success_criteria>The Instrument slice now controls real persisted project resources through the selected host.</success_criteria>
<output>Create `.planning/phases/01-category-proof/01-08-SUMMARY.md` when done.</output>
