---
phase: 01-category-proof
plan: "13"
type: execute
wave: 13
depends_on: ["01-12"]
files_modified: [crates/preview-supervisor/Cargo.toml, crates/preview-supervisor/src/lib.rs, crates/preview-supervisor/tests/preview_lifecycle.rs, packages/contracts/src/desktop-extension.ts, apps/desktop/host.config.json, apps/desktop/src-tauri/src/commands.rs, apps/desktop/electron/main.ts, apps/desktop/electron/preload.ts, apps/desktop/electron/supervisor.ts, tests/contracts/selected-host-conformance.test.ts, apps/renderer/src/features/preview/PreviewSurface.tsx]
autonomous: true
requirements: [WORK-04, LIFE-01]
must_haves:
  truths:
    - "Preview failure/recovery carries exact run/effect/artifact causation and preserves evidence."
    - "Generated content cannot reach IDE authority, privileged navigation, or uncontrolled windows."
  artifacts:
    - {path: "crates/preview-supervisor/src/lib.rs", provides: "Owned causal preview lifecycle"}
    - {path: "apps/renderer/src/features/preview/PreviewSurface.tsx", provides: "Isolated preview and correlated errors"}
  key_links:
    - {from: "crates/preview-supervisor/src/lib.rs", to: "apps/desktop/host.config.json", via: "Selected Tauri command owner or Electron main/preload/supervisor"}
    - {from: "packages/contracts/src/desktop-extension.ts", to: "apps/renderer/src/features/preview/PreviewSurface.tsx", via: "Validated run/effect/artifact lifecycle DTOs"}
---
<objective>Supervise the executable benchmark preview and correlate failure/recovery causally.

Purpose: Complete WORK-04 through the selected host while preserving D-09 and R-03.
Output: Preview supervisor, selected-host bridge, isolated Preview Surface, and lifecycle proof.</objective>
<execution_context>@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-category-proof/01-12-SUMMARY.md
@docs/decisions/0001-desktop-shell.md</context>
## Phase Goal
**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Isolate and correlate preview lifecycle</name>
  <files>crates/preview-supervisor/Cargo.toml, crates/preview-supervisor/src/lib.rs, crates/preview-supervisor/tests/preview_lifecycle.rs, packages/contracts/src/desktop-extension.ts, apps/desktop/host.config.json, apps/desktop/src-tauri/src/commands.rs, apps/desktop/electron/main.ts, apps/desktop/electron/preload.ts, apps/desktop/electron/supervisor.ts, tests/contracts/selected-host-conformance.test.ts, apps/renderer/src/features/preview/PreviewSurface.tsx</files>
  <behavior>
    - Starting, healthy, stale, broken, reconnecting carry run/cause/effect/artifact revisions, bounded logs, and error metadata.
    - Failure links by IDs, never timestamp proximity; recovery retains the prior failure receipt.
    - Preview has separate origin/partition, no bridge, denied privileged navigation/windows, and owned cleanup.
  </behavior>
  <action>Implement owned process/health/reload/error/reconnect lifecycle and bounded raw logs. Read `host.config.json` and modify only the winner's recorded owners: Tauri commands/webview policy in `src-tauri/src/commands.rs`, or Electron process/preload/supervisor isolation in `electron/{main.ts,preload.ts,supervisor.ts}`. Extend the winner-neutral extension contract and conformance suite. Render an isolated surface with no privileged bridge and link errors to Activity Strip/artifacts only from explicit IDs. Apply only platform cleanup guarantees established by R-03.</action>
  <verify><automated>rtk cargo test -p preview-supervisor --test preview_lifecycle -- --test-threads=1 &amp;&amp; rtk npm test -- --run PreviewSurface tests/contracts/selected-host-conformance.test.ts &amp;&amp; rtk npm run test:journey -- --grep "preview error correlation"</automated></verify>
  <done>The preview fails, recovers, remains isolated, and traces every visible error to exact activity/artifact evidence.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Generated preview→desktop chrome | Arbitrary product content attempts host access. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-16 | Elevation of privilege | preview surface | critical | mitigate | Separate origin/partition, no bridge, CSP, navigation/window denial. |
| T-01-19 | Spoofing | error causation | high | mitigate | Immutable run/cause/effect/artifact IDs emitted before launch. |
</threat_model>
<verification>Run lifecycle, isolation, renderer, and journey correlation tests.</verification>
<success_criteria>Preview behavior is executable, recoverable, safely isolated, and causally explained.</success_criteria>
<output>Create `.planning/phases/01-category-proof/01-13-SUMMARY.md` when done.</output>
