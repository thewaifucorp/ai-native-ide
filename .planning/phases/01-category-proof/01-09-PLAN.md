---
phase: 01-category-proof
plan: "09"
type: execute
wave: 9
depends_on: ["01-08"]
files_modified: [crates/pty-runtime/Cargo.toml, crates/pty-runtime/src/lib.rs, crates/pty-runtime/tests/pty_lifecycle.rs, packages/contracts/src/desktop-extension.ts, apps/desktop/host.config.json, apps/desktop/src-tauri/src/commands.rs, apps/desktop/electron/main.ts, apps/desktop/electron/preload.ts, apps/desktop/electron/supervisor.ts, apps/renderer/src/features/workspace/terminal-adapter.ts, apps/renderer/src/features/workspace/TerminalSurface.tsx, tests/contracts/selected-host-conformance.test.ts, tests/journey/host-pty.spec.ts]
autonomous: true
requirements: [LIFE-01]
must_haves:
  truths:
    - "A real PTY streams, resizes, cancels, observes exit, bounds output, and cleans Linux descendants."
    - "Raw terminal output stays available and cannot execute renderer HTML or privileged navigation."
  artifacts:
    - {path: "crates/pty-runtime/src/lib.rs", provides: "Rust PTY lifecycle"}
    - {path: "apps/renderer/src/features/workspace/terminal-adapter.ts", provides: "Renderer adapter for typed PTY commands and events"}
    - {path: "tests/journey/host-pty.spec.ts", provides: "Host-backed PTY proof"}
  key_links:
    - {from: "crates/pty-runtime/src/lib.rs", to: "apps/desktop/host.config.json", via: "Selected Tauri command registry or Electron main/preload/supervisor"}
    - {from: "packages/contracts/src/desktop-extension.ts", to: "apps/renderer/src/features/workspace/terminal-adapter.ts", via: "spawn/input/resize/cancel and output/exit DTO streams"}
    - {from: "apps/renderer/src/features/workspace/terminal-adapter.ts", to: "apps/renderer/src/features/workspace/TerminalSurface.tsx", via: "Bounded host events rendered as untrusted text"}
---
<objective>Provide the real supervised PTY required by D-03.

Purpose: Establish truthful process lifecycle before external agents depend on it.
Output: PTY runtime, lifecycle tests, and safe xterm surface.</objective>
<execution_context>@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-category-proof/01-RESEARCH.md
@.planning/phases/01-category-proof/01-08-SUMMARY.md</context>
## Phase Goal
**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement and render the supervised PTY</name>
  <files>crates/pty-runtime/Cargo.toml, crates/pty-runtime/src/lib.rs, crates/pty-runtime/tests/pty_lifecycle.rs, packages/contracts/src/desktop-extension.ts, apps/desktop/host.config.json, apps/desktop/src-tauri/src/commands.rs, apps/desktop/electron/main.ts, apps/desktop/electron/preload.ts, apps/desktop/electron/supervisor.ts, apps/renderer/src/features/workspace/terminal-adapter.ts, apps/renderer/src/features/workspace/TerminalSurface.tsx, tests/contracts/selected-host-conformance.test.ts, tests/journey/host-pty.spec.ts</files>
  <behavior>
    - Echo, streaming, resize, exit code, cancellation, 10 MB backpressure, and Linux process-group cleanup are observed.
    - Cancellation completes only after exit; Windows/macOS guarantees remain unknown unless executed.
  </behavior>
  <action>Confirm `portable-pty` source/identity, pin it, and implement bounded channels, Linux process-group ownership, graceful then forced termination, exit reaping, and state transitions. Add closed DTOs for spawn/input/resize/cancel commands and output/exit events. Read `host.config.json` and modify only the selected owner: Tauri commands/channel in `src-tauri/src/commands.rs`, or Electron handlers/preload plus lifecycle ownership in `electron/{main.ts,preload.ts,supervisor.ts}`. Wire the renderer `terminal-adapter.ts` to TerminalSurface; render through xterm as untrusted text, restrict links, and expose complete Raw output. Extend the one selected-host conformance suite and a desktop-host E2E that spawns a shell, sends input, observes output, resizes, cancels, observes exit, and proves cleanup. Record unsupported platform claims honestly per R-03.</action>
  <verify><automated>rtk cargo test -p pty-runtime --test pty_lifecycle -- --test-threads=1 &amp;&amp; rtk npm test -- --run TerminalSurface tests/contracts/selected-host-conformance.test.ts &amp;&amp; rtk npm run test:journey -- --grep "host-backed PTY lifecycle"</automated></verify>
  <done>Interactive terminal behavior and Linux cleanup are empirically proven without overclaiming other platforms.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| PTY output→renderer | Arbitrary process bytes reach UI and links. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-13 | Elevation of privilege | terminal output | high | mitigate | xterm text rendering, controlled links, no HTML or host bridge. |
| T-01-06 | Denial of service | PTY | high | mitigate | Bounded backpressure, cancellation escalation, process-group cleanup. |
</threat_model>
<verification>Run lifecycle and renderer tests, including large output, cancel, close, and orphan checks.</verification>
<success_criteria>A real, observable, safely rendered PTY is available to adapters.</success_criteria>
<output>Create `.planning/phases/01-category-proof/01-09-SUMMARY.md` when done.</output>
