---
phase: 01-category-proof
plan: "07"
type: execute
wave: 7
depends_on: ["01-06"]
files_modified: [Cargo.toml, crates/ide-core/Cargo.toml, crates/ide-core/src/lib.rs, crates/ide-core/src/project.rs, crates/ide-core/src/resources.rs, crates/ide-core/src/activity.rs, crates/ide-core/tests/project_resource.rs]
autonomous: true
requirements: [PROJ-01, INTN-01, CONF-01, LIFE-01]
must_haves:
  truths:
    - "A semantic project persists independently of repository path and session."
    - "Scoped reads/writes reject traversal and distinguish IDE-caused from external changes."
  artifacts:
    - {path: "crates/ide-core/src/project.rs", provides: "Semantic project persistence"}
    - {path: "crates/ide-core/src/resources.rs", provides: "Scoped file effects and observation"}
  key_links:
    - {from: "crates/ide-core/src/activity.rs", to: "crates/ide-core/src/resources.rs", via: "Cause/effect/resource/revision IDs"}
---
<objective>Implement real local semantic-project and resource authority in Rust.

Purpose: Replace fake state without making a repo or conversation the project container, per D-03 and D-05.
Output: ide-core persistence, scoped filesystem effects, causal activity, and integration tests.</objective>
<execution_context>@/home/mario/.codex/gsd-core/workflows/execute-plan.md
@/home/mario/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-category-proof/SKELETON.md
@.planning/phases/01-category-proof/01-RESEARCH.md
@.planning/phases/01-category-proof/01-06-SUMMARY.md</context>
## Phase Goal
**As a** nontechnical builder, **I want to** describe and build an executable sealed-bid leaderboard through one guided desktop journey, **so that** I can control a real software result without first configuring a technical workspace.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Persist project, resources, activity, and revisions</name>
  <files>Cargo.toml, crates/ide-core/Cargo.toml, crates/ide-core/src/lib.rs, crates/ide-core/src/project.rs, crates/ide-core/src/resources.rs, crates/ide-core/src/activity.rs, crates/ide-core/tests/project_resource.rs</files>
  <behavior>
    - Intent creates/reopens a manifest and SQLite record keyed by project ID with stable resource IDs.
    - Canonicalization and symlink resolution confine reads/writes to declared roots.
    - Atomic writes emit linked effects/revisions; external edits retain unknown causation.
  </behavior>
  <action>Implement Serde domain types, migrations, editable manifest, explicit local data directory, resource roots, atomic replacement, revision hashes, watch observation, and causal activity. Reject traversal, symlink escape, stale revisions, and renderer-supplied path authority. Keep files/manifests editable truth and SQLite as index/ledger, per D-05.</action>
  <verify><automated>rtk cargo test -p ide-core --test project_resource</automated></verify>
  <done>The core creates/reopens a semantic project, safely edits a resource, and honestly observes an external change.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Resource scope→filesystem | Paths and symlinks can escape authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-07 | Tampering | resource paths | critical | mitigate | Canonical root containment, symlink handling, stable resource IDs, stale-revision rejection. |
| T-01-08 | Repudiation | activity | medium | mitigate | Immutable IDs/revisions and explicit unknown external causation. |
</threat_model>
<verification>Run the core integration test including traversal, symlink, stale-write, reopen, and external-edit cases.</verification>
<success_criteria>Real local project/file authority exists behind a narrow Rust boundary.</success_criteria>
<output>Create `.planning/phases/01-category-proof/01-07-SUMMARY.md` when done.</output>

